import java.util.Locale;

public class Main {

	public static void main(String[] args) {
		
		int y = 32;
		double x = 10.35784;
		
		System.out.println("Bom dia!");
		
		System.out.println(y);
		// 'print' mostra o valor da variável , enquanto 'println' inclui uma quebra de linha ao final
		
		System.out.println(x);
		/* double permite valores com ponto flutuante
		para definir quantas casas decimais devemos usar o 'printf' (de formatado), da seguinte forma:
		printf("%.Nf%n", x), onde N é o número de casas a serem consideradas, e x é o valor da variável.
		O padrão para essa operação é apenas '%.Nf', %n ou \n representa quebra de linha ao final da mensagem*/
		
		System.out.printf("%.2f%n", x);
		System.out.printf("%.4f", x);
		System.out.printf(" %.2f%n", x);
		/* O separador de casas decimais é ',', uma vez que o comando 'f' utiliza a configuração do computador 
		podendo ser POR, ENG, etc. Para configurar o separador como sendo "." (separados americano), devemos
		aplicar a seguinte configuração:*/
		
		Locale.setDefault(Locale.US);
		/* Locale é uma classe do JDK, mas não é reconhecida pelo sistema. Para que reconheça, é necessário
		importar essa classe no topo da aplicação 'import java.util.Locale;' ou passar o mause onde está o erro 
		e seleciona 'import Locale (java.util)', que o eclipse adiciona automaticamente no topo*/

		System.out.printf("%.4f", x);
		// Após importação da classe Locale, a aplicação já reconhece o separador como sendo o americano ('.')

		System.out.println("RESULTADO = " + x + " metros");
		// Concatenando elementos
		System.out.printf("RESULTADO = %.2f metros%n", x);
		// Aplicando printf
		
	
	}

}
